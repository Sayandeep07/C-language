#include<stdio.h>
int main() {
    int a, b, area;
    printf("Enter the value of both sides:\n");
    scanf("%d %d", &a, &b);
    area = a * b;
    if (a == b) {
        printf("Area of Square is: %d\n", area);
    } else {
        printf("Area of Rectangle is: %d\n", area);
    }
}
