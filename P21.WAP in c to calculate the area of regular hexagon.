#include<stdio.h>
#include<math.h>

int main() {
    float side, area;
    printf("Enter the length of side: ");
    scanf("%f", &side);
    area = (3 * sqrt(3) / 2) * (side * side);
    printf("Area of hexagon is: %f\n", area);
}
