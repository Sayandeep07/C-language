#include<stdio.h>
int main()
{
int a,b,hcf=1;
printf("Enter the value of A:\n");
scanf("%d",&a);
printf("Enter the value of B:\n");
scanf("%d",&b);
for(int i = 1; i <=a || i <= b; i++) 
{
if(a % i == 0 && b % i == 0)
hcf = i;
}
printf("The HCF: %d", hcf);
}
